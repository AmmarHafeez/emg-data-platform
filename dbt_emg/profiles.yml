emg_data_platform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'emg_admin') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'emg_admin') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'emg_platform') }}"
      schema: analytics
      threads: 4
      keepalives_idle: 0
